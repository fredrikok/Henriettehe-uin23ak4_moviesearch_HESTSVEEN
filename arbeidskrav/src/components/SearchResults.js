import MovieCards from "./MovieCards";

export default function SearchResults(){

    
    handleSubmit = {filter};

    return (
        //form - som går {handleSubmit}
        <MovieCards />
        //event.target
    )
}